<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Community Aid</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Raleway:ital,wght@0,100..900;1,100..900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/footer.css" />
    <link rel="stylesheet" href="/css/home.css" />
    <link rel="stylesheet" href="/css/loader.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      body {
        font-family: 'Raleway', sans-serif;
        background: linear-gradient(135deg, #ffebee, #ffffff);
      }
    </style>
  </head>
  <body class="min-h-screen flex flex-col">
    <%-include('../partials/loader')%>
    <%- include('../partials/navbar') %>
    <br><br><br>
    <main class="flex-grow flex justify-center items-center px-4 py-8">
      <div class="chat-window w-full max-w-4xl bg-white rounded-2xl shadow-xl border-4 border-orange-400 overflow-hidden animate-fade-in">
        <div class="bg-red-800 text-white text-center py-4 text-lg font-semibold">
          🌍 Crisis Response Chatbot
          <button class="close float-right mr-4 bg-white text-red-800 px-2 py-1 rounded">X</button>
        </div>
        <div class="chat p-4 bg-red-50 border-l-4 border-orange-400 h-96 overflow-y-auto space-y-4">
          <div class="flex justify-start">
            <p class="bg-orange-200 text-gray-800 p-3 rounded-xl shadow max-w-[75%] animate-slide-in">
              ⚡ Hello! How can I assist you in this emergency situation?
            </p>
          </div>
        </div>
        <div class="input-area flex items-center gap-2 border-t-2 border-orange-400 p-4 bg-white">
          <input
            type="text"
            placeholder="Ask for help or information..."
            class="flex-grow px-4 py-2 rounded-full bg-orange-100 focus:outline-none focus:ring-2 focus:ring-orange-300 text-sm"
          />
          <button
            class="w-11 h-11 flex items-center justify-center bg-red-700 hover:bg-orange-500 text-white rounded-full transition-transform duration-150 transform hover:scale-110 animate-bounce"
            aria-label="Send message"
          >
            <img src="/img/senqsd-icon.png" alt="Send" class="w-5 h-5" />
          </button>
        </div>
        <div class="typing-indicator hidden px-4 pb-2 text-gray-500 text-sm">AI is typing...</div>
      </div>
    </main>
    <%-include('../partials/footer')%>

    <script>
      // Inject your Gemini API key securely here (for demo only, use env vars in production)
      window.GEMINI_API_KEY = "<%= process.env.GEMINI_API_KEY || 'YOUR_GEMINI_API_KEY' %>";
    </script>
    <script type="importmap">
      {
        "imports": {
          "@google/generative-ai": "https://esm.run/@google/generative-ai"
        }
      }
    </script>
    <script type="module" src="/js/chatbot.js"></script>
    <script src="/js/loader.js"></script>
  </body>
</html>
